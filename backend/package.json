{
  "name": "rittik-portfolio-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Production server for Rittik Pati's portfolio — serves the built frontend and exposes a minimal health API.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
